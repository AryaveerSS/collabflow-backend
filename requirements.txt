# ================================
# CollabFlow Backend - Requirements
# ================================

# --- Core Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # for file uploads & form data

# --- Database ---
sqlalchemy==2.0.30             # async ORM
asyncpg==0.29.0                # async PostgreSQL driver
alembic==1.13.1                # database migrations
psycopg2-binary==2.9.9         # sync driver (used by alembic)

# --- Redis ---
redis==5.0.4                   # Redis client (async support)

# --- Auth & Security ---
passlib[bcrypt]==1.7.4         # password hashing
python-jose[cryptography]==3.3.0  # JWT tokens
bcrypt==4.1.3

# --- Validation & Settings ---
pydantic==2.7.1                # request/response validation
pydantic-settings==2.2.1       # settings from .env file
email-validator==2.1.1         # email format validation

# --- HTTP Client (for testing & internal calls) ---
httpx==0.27.0

# --- WebSockets ---
websockets==12.0

# --- Background Jobs ---
celery==5.4.0                  # async task queue
flower==2.0.1                  # celery monitoring UI

# --- File Handling ---
python-magic==0.4.27           # file type detection
aiofiles==23.2.1               # async file I/O

# --- Logging ---
loguru==0.7.2                  # clean structured logging

# --- Utilities ---
python-dotenv==1.0.1           # load .env files
pytz==2024.1                   # timezone handling
slugify==0.0.1                 # slug generation

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
httpx==0.27.0                  # async test client for FastAPI
factory-boy==3.3.0             # test data factories
faker==25.0.1                  # fake data generation